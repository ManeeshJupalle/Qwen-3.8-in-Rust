# GGUF tokenizer/template vs HF files; generated by an inline script in the Phase 0 session

chat template identical to HF chat_template.jinja: False (gguf 9993 chars, hf 8952 chars)
unified diff (87 lines):
  --- HF chat_template.jinja
  +++ GGUF tokenizer.chat_template
  @@ -44,2 +44,14 @@
   {%- endif %}
  +{%- set sysns = namespace(count=0, text='') %}
  +{%- for message in messages %}
  +    {%- if sysns.count == loop.index0 and (message.role == 'system' or message.role == 'developer') %}
  +        {%- set sys_content = render_content(message.content, false, true)|trim %}
  +        {%- if sys_content %}
  +            {%- set sysns.text = sysns.text + ('\n' if sysns.text else '') + sys_content %}
  +        {%- endif %}
  +        {%- set sysns.count = sysns.count + 1 %}
  +    {%- endif %}
  +{%- endfor %}
  +{%- set num_sys = sysns.count %}
  +{%- set merged_system = sysns.text %}
   {%- set reasoning_instructions = '' %}
  @@ -47,2 +59,5 @@
       {%- set resolved_reasoning_effort = reasoning_effort|default('xhigh') %}
  +    {%- if resolved_reasoning_effort == 'high' %}
  +        {%- set resolved_reasoning_effort = 'xhigh' %}
  +    {%- endif %}
       {%- if resolved_reasoning_effort not in ('xhigh', 'medium', 'low') %}
  @@ -68,7 +83,4 @@
       {{- '\n\nIf you choose to call a function ONLY reply in the following format with NO suffix:\n\n<tool_call>\n<function=example_function_name>\n<parameter=example_parameter_1>\nvalue_1\n</parameter>\n<parameter=example_parameter_2>\nThis is the value for the second parameter\nthat can span\nmultiple lines\n</parameter>\n</function>\n</tool_call>\n\n<IMPORTANT>\nReminder:\n- Function calls MUST follow the specified format: an inner <function=...></function> block must be nested within <tool_call></tool_call> XML tags\n- Required parameters MUST be specified\n- You may provide optional reasoning for your function call in natural language BEFORE the function call, but NOT after\n- If there is no function call available, answer the question like normal with your current knowledge and do not tell the user about function calls\n</IMPORTANT>' }}
  -    {%- if messages[0].role == 'system' %}
  -        {%- set content = render_content(messages[0].content, false, true)|trim %}
  -        {%- if content %}
  -            {{- '\n\n' + content }}
  -        {%- endif %}
  +    {%- if merged_system %}
  +        {{- '\n\n' + merged_system }}
       {%- endif %}
  @@ -76,9 +88,4 @@
   {%- else %}
  -    {%- if messages[0].role == 'system' %}
  -        {%- set content = render_content(messages[0].content, false, true)|trim %}
  -        {%- if content %}
  -            {{- '<|im_start|>system\n' + (reasoning_instructions + '\n\n' if reasoning_instructions else '')  + content + '<|im_end|>\n' }}
  -        {%- elif reasoning_instructions %}
  -            {{- '<|im_start|>system\n' + reasoning_instructions + '<|im_end|>\n' }}
  -        {%- endif %}
  +    {%- if merged_system %}
  +        {{- '<|im_start|>system\n' + (reasoning_instructions + '\n\n' if reasoning_instructions else '')  + merged_system + '<|im_end|>\n' }}
       {%- elif reasoning_instructions %}
  @@ -98,11 +105,7 @@
   {%- endfor %}
  -{%- if ns.multi_step_tool %}
  -    {{- raise_exception('No user query found in messages.') }}
  -{%- endif %}
   {%- for message in messages %}
  +    {%- if loop.index0 >= num_sys %}
       {%- set content = render_content(message.content, true)|trim %}
  -    {%- if message.role == "system" %}
  -        {%- if not loop.first %}
  -            {{- raise_exception('System message must be at the beginning.') }}
  -        {%- endif %}
  +    {%- if message.role == "system" or message.role == "developer" %}
  +        {{- raise_exception('System message must be at the beginning.') }}
       {%- elif message.role == "user" %}
  @@ -125,2 +128,5 @@
                   {%- endif %}
  +                {%- if tool_call.name is not defined or tool_call.name is none %}
  +                    {{- raise_exception('Tool call is missing a function name.') }}
  +                {%- endif %}
                   {%- if loop.first %}
  @@ -134,3 +140,3 @@
                   {%- endif %}
  -                {%- if tool_call.arguments is defined and tool_call.arguments != '' %}
  +                {%- if tool_call.arguments is mapping %}
                       {%- for args_name, args_value in tool_call.arguments|items %}
  @@ -141,2 +147,8 @@
                       {%- endfor %}
  +                {%- elif tool_call.arguments is string %}
  +                    {%- if tool_call.arguments|trim %}
  +                        {{- raise_exception('Tool call arguments for function "' + (tool_call.name | string) + '" were passed as a JSON string. Parse them into an object before calling apply_chat_template.') }}
  +                    {%- endif %}
  +                {%- elif tool_call.arguments is defined and tool_call.arguments is not none %}
  +                    {{- raise_exception('Tool call arguments for function "' + (tool_call.name | string) + '" must be an object/mapping or a JSON string.') }}
                   {%- endif %}
  @@ -161,2 +173,3 @@
       {%- endif %}
  +    {%- endif %}
   {%- endfor %}
  @@ -170 +183,2 @@
   {%- endif %}
  +{#- Unsloth fixes - developer role, merged system messages, tool calling #}

gguf tokens: 248320 ; hf vocab: 248044 ; hf added_tokens: 33 ; hf max id+1: 248077
ids where gguf token != hf token (by id): 243
  of which hf has no token (padding ids): 243, range 248077..248319, gguf strings e.g. ['[PAD248077]', '[PAD248078]', '[PAD248079]']
  real mismatches: 0 []
token_type histogram: {1: 248044, 3: 27, 4: 6, 5: 243}
token_type by id range: ids with type!=1: [(248044, 3, '<|endoftext|>'), (248045, 3, '<|im_start|>'), (248046, 3, '<|im_end|>'), (248047, 3, '<|object_ref_start|>'), (248048, 3, '<|object_ref_end|>'), (248049, 3, '<|box_start|>'), (248050, 3, '<|box_end|>'), (248051, 3, '<|quad_start|>'), (248052, 3, '<|quad_end|>'), (248053, 3, '<|vision_start|>'), (248054, 3, '<|vision_end|>'), (248055, 3, '<|vision_pad|>'), (248056, 3, '<|image_pad|>'), (248057, 3, '<|video_pad|>'), (248058, 4, '<tool_call>'), (248059, 4, '</tool_call>'), (248060, 3, '<|fim_prefix|>'), (248061, 3, '<|fim_middle|>'), (248062, 3, '<|fim_suffix|>'), (248063, 3, '<|fim_pad|>'), (248064, 3, '<|repo_name|>'), (248065, 3, '<|file_sep|>'), (248066, 4, '<tool_response>'), (248067, 4, '</tool_response>'), (248068, 4, '<think>'), (248069, 4, '</think>'), (248070, 3, '<|audio_start|>'), (248071, 3, '<|audio_end|>'), (248072, 3, '<tts_pad>'), (248073, 3, '<tts_text_bos>'), (248074, 3, '<tts_text_eod>'), (248075, 3, '<tts_text_bos_single>'), (248076, 3, '<|audio_pad|>'), (248077, 5, '[PAD248077]'), (248078, 5, '[PAD248078]'), (248079, 5, '[PAD248079]'), (248080, 5, '[PAD248080]'), (248081, 5, '[PAD248081]'), (248082, 5, '[PAD248082]'), (248083, 5, '[PAD248083]')]

tokenizer.* keys present: ['tokenizer.ggml.model', 'tokenizer.ggml.pre', 'tokenizer.ggml.tokens', 'tokenizer.ggml.token_type', 'tokenizer.ggml.merges', 'tokenizer.ggml.eos_token_id', 'tokenizer.ggml.padding_token_id', 'tokenizer.ggml.bos_token_id', 'tokenizer.chat_template']
tokenizer.ggml.add_bos_token present: False
tokenizer.ggml.add_eos_token present: False
bos=248044 eos=248046 pad=248055
merges: gguf 247587 vs hf 247587; first-100 identical: True
