[package]
name = "aqueduct"
version = "0.1.0"
edition = "2021"

[dependencies]
aqueduct-core = { path = "../core" }

[features]
# Forwarded to aqueduct-core; `aqueduct run --profile` needs it.
profile = ["aqueduct-core/profile"]
