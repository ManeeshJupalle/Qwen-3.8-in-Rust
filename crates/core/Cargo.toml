[package]
name = "aqueduct-core"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "2"
half = "2"
tokenizers = { version = "0.23", default-features = false, features = ["fancy-regex"] }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
